import { ReverseFilterPipe } from './reverse-filter.pipe';

describe('ReverseFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ReverseFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
